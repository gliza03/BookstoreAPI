export interface Book {
    bookID: number;
    title: string;
    author: string;
    publisher: number;
    isbn: string;
    classification: string;
    category: string;
    pageCount: number;
    price: number;
}
